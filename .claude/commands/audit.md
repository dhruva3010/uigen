Your task is to udpate any vulnerable dependencies.

Do the following:

1. Run `npm audit` to identify vulnerable dependencies.
2. Run `npm audit fix` to automatically fix vulnerabilities where possible.
3. Run tests to ensure nothing is broken after the updates.
